/** Fiscal / demographic context for the tax unit (enriched in later engine stories). */
export type HouseholdProfile = {
  /** Année civile d’imposition / de simulation (ex. 2026). */
  taxYear: number;
};
