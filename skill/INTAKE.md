# Intake — ordre des questions (squelette)

1. **Mode de garde** visé parmi les slugs : `creche-publique`, `creche-berceau-employeur`, `assistante-maternelle`, `nounou-domicile`.
2. **Foyer** : nombre d’enfants, répartition des revenus (à affiner quand le moteur exposera les champs).
3. **Paramètres spécifiques** : lire `src/scenarios/<slug>/params.md` pour la liste à collecter.

Ensuite : `node scripts/simulate.mjs <slug>` et présenter le **tableau** de sortie enrichi des explications utilisateur.
